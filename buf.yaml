# buf.yaml – Module configuration for buf
# https://buf.build/docs/configuration/v2/buf-yaml
version: v2

modules:
  - path: proto
    name: buf.build/qualithm/arrow-flight-client

deps:
  - buf.build/protocolbuffers/wellknowntypes

lint:
  use:
    - STANDARD

breaking:
  use:
    - FILE
